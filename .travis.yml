language: java
sudo: false
dist: trusty
jdk:
  - oraclejdk8
  - openjdk8
branches:
  only:
  - master
  - develop
before_install:
  - git clone https://github.com/LearnLib/automatalib.git --single-branch develop /tmp/automatalib-develop
  - pushd /tmp/automatalib-develop
  - mvn clean install
  - popd
script:
  - mvn verify -B -Pintergration-tests